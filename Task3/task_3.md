# Завдання 3: Візуалізація структури директорії з кольоровим виведенням

## Умова

Розробіть скрипт, який приймає **шлях до директорії** в якості аргументу командного рядка та **візуалізує її структуру**, виводячи імена всіх **піддиректорій та файлів** з різними кольорами для кращого візуального сприйняття.

## Вимоги

* Створіть **віртуальне оточення Python** для ізоляції залежностей проєкту.
* Скрипт має **отримувати шлях до директорії** як аргумент командного рядка.
* Використовуйте **бібліотеку `colorama`** для реалізації кольорового виведення:

  * один колір — для директорій,
  * інший — для файлів.
* Використовуйте **рекурсивний обхід директорій** (або ітеративний за бажанням).
* Застосуйте **перевірку на існування шляху** та його тип (чи це директорія).

## Рекомендації до виконання

* Створіть та активуйте **віртуальне середовище**:

  ```bash
  python -m venv venv
  source venv/bin/activate  # або venv\Scripts\activate для Windows
  pip install colorama
  ```

* Використовуйте:

  * `sys.argv` — для отримання аргументу командного рядка,
  * `pathlib.Path` — для навігації по директоріях,
  * `colorama.Fore`, `colorama.Style` — для кольорів.

* Використовуйте чітку індентацію або графічні символи для структури.

## Критерії оцінювання

* Правильне створення і використання віртуального середовища.
* Коректне отримання та обробка шляху до директорії.
* Точне виведення структури директорії.
* Застосування кольорового форматування через `colorama`.
* Чистота, структура і зрозумілість коду.

## Приклад використання

```bash
python hw03.py /шлях/до/вашої/директорії
```

### Вхідна структура директорії:

```
📦picture
 ┣ 📂Logo
 ┃ ┣ 📜IBM+Logo.png
 ┃ ┣ 📜ibm.svg
 ┃ ┗ 📜logo-tm.png
 ┣ 📜bot-icon.png
 ┗ 📜mongodb.jpg
```

### Очікуваний результат у терміналі (кольори для файлів та директорій відрізняються):

```
picture/
├── Logo/
│   ├── IBM+Logo.png
│   ├── ibm.svg
│   └── logo-tm.png
├── bot-icon.png
└── mongodb.jpg
```

> **Примітка:** кольори мають бути реалізовані через `colorama`, наприклад:
>
> * `Fore.BLUE` для директорій,
> * `Fore.GREEN` для файлів,
> * `Style.RESET_ALL` після кожного рядка.
